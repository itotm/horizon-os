---
name: Build container image (dev)
on:
  workflow_dispatch:
    inputs:
      fedora_version:
        description: "Fedora base image"
        required: false
        default: "44"
        type: choice
        options:
          - "44"
          - "45"

env:
  IMAGE_DESC: "HorizonOS - Customized Fedora Kinoite Image (Dev)"
  IMAGE_KEYWORDS: "bootc,fedora,kinoite,atomic,dev"
  IMAGE_LOGO_URL: "https://horizon-os.itotm.org/logo.png"
  IMAGE_NAME: "${{ github.event.repository.name }}"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "dev"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build_push:
    name: Build and push image
    runs-on: ubuntu-24.04

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Prepare environment
        run: |
          # Lowercase the image uri
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Get current date and version
        id: date
        run: |
          echo "date=$(date -u +%Y\-%m\-%d\T%H\:%M\:%S\Z)" >> $GITHUB_OUTPUT
          BUILD_DATE=$(date -u +%Y%m%d)
          BUILD_NUMBER=${{ github.run_number }}
          VERSION="${BUILD_DATE}.${BUILD_NUMBER}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Image Metadata
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        id: metadata
        with:
          tags: |
            type=raw,value=${{ env.DEFAULT_TAG }}
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/blob/${{ github.sha }}/Containerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/tree/${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ steps.date.outputs.version }}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.IMAGE_LOGO_URL }}
            io.artifacthub.package.prerelease=true
            containers.bootc=1
          sep-tags: " "
          sep-annotations: " "

      - name: Build Image
        id: build_image
        run: |
          sudo buildah build \
            --file ./Containerfile \
            --tag ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }} \
            --build-arg FEDORA_VERSION=${{ github.event.inputs.fedora_version || '44' }} \
            --build-arg BUILD_NUMBER=${{ github.run_number }} \
            --build-arg IMAGE_VERSION=${{ steps.date.outputs.version }} \
            --build-arg ENABLE_COMMON=true \
            --build-arg ENABLE_STANDARD=true \
            --build-arg ENABLE_EXTENDED=false \
            --build-arg ENABLE_VIRTTOOLS=true \
            --build-arg ENABLE_DEVTOOLS=true \
            --build-arg ENABLE_EXPERIMENTAL=false \
            --build-arg DISABLE_REPOS=true \
            --label "${{ steps.metadata.outputs.labels }}" \
            .

      - name: Log image size
        run: |
          IMAGE_ID=$(sudo buildah images --format "{{.ID}}" ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }} | head -n 1)
          IMAGE_SIZE=$(sudo buildah images --format "{{.Size}}" ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }} | head -n 1)
          echo "Built image ID: $IMAGE_ID"
          echo "Image size: $IMAGE_SIZE"

      - name: Push To GHCR
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | sudo podman login ghcr.io -u ${{ github.actor }} --password-stdin
          for tag in ${{ steps.metadata.outputs.tags }}; do
            sudo skopeo copy containers-storage:${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:$tag docker://${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:$tag
          done

      - name: Delete old images from GHCR
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          PACKAGE_NAME="${{ env.IMAGE_NAME }}"
          CUTOFF_DATE=$(date -u -d '8 days ago' +%s)

          echo "Fetching versions for ${PACKAGE_NAME}..."
          PAGE=1
          while true; do
            RESPONSE=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/user/packages/container/${PACKAGE_NAME}/versions?per_page=100&page=${PAGE}")

            COUNT=$(echo "$RESPONSE" | jq 'length')
            if [ "$COUNT" = "0" ] || [ -z "$COUNT" ]; then
              break
            fi

            echo "$RESPONSE" | jq -c '.[]' | while read -r VERSION; do
              VERSION_ID=$(echo "$VERSION" | jq -r '.id')
              TAGS=$(echo "$VERSION" | jq -r '.metadata.container.tags // [] | join(",")')
              UPDATED_AT=$(echo "$VERSION" | jq -r '.updated_at')
              VERSION_TS=$(date -u -d "$UPDATED_AT" +%s 2>/dev/null || echo 0)

              # Skip if it has the default tag
              if echo "$TAGS" | grep -q "${{ env.DEFAULT_TAG }}"; then
                continue
              fi

              if [ "$VERSION_TS" -lt "$CUTOFF_DATE" ] 2>/dev/null; then
                echo "Deleting version ID: ${VERSION_ID} (tags: ${TAGS}, updated: ${UPDATED_AT})"
                curl -s -X DELETE \
                  -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                  -H "Accept: application/vnd.github+json" \
                  "https://api.github.com/user/packages/container/${PACKAGE_NAME}/versions/${VERSION_ID}"
              fi
            done

            if [ "$COUNT" -lt 100 ]; then
              break
            fi
            PAGE=$((PAGE + 1))
          done

          echo "GHCR cleanup complete."
